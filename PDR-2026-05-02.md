# تقرير التطوير اليومي — التحليل الشامل
## نظام Tracker — وحدة التجارة المحلية
### تاريخ: السبت 2 مايو 2026

---

## الملخص التنفيذي

| البند | التفاصيل |
|---|---|
| **مدة الجلسة** | 11:35 ص — 3:15 م (3 ساعات و40 دقيقة) |
| **عدد الـ Commits** | 25 commit |
| **إجمالي الأسطر المضافة** | ~4,200 سطر |
| **إجمالي الأسطر المحذوفة** | ~600 سطر |
| **الوحدة المستهدفة** | التجارة المحلية (Local Trade Module) |
| **الملفات الأساسية المعدّلة** | 9 ملفات |

### حجم كل ملف بعد اليوم

| الملف | عدد الأسطر |
|---|---|
| `server/storage.ts` | 4,921 سطر |
| `server/routes.ts` | 3,291 سطر |
| `client/src/pages/local-trade/party-profile.tsx` | 3,778 سطر |
| `client/src/pages/local-trade/invoices.tsx` | 1,186 سطر |
| `client/src/pages/local-trade/create-invoice.tsx` | 887 سطر |
| `client/src/pages/local-trade/parties.tsx` | 545 سطر |
| `client/src/hooks/use-local-trade.ts` | 439 سطر |
| `shared/schema.ts` | 837 سطر |

---

## الفهرس التسلسلي للتغييرات

1. [توثيق PRD الوحدة كاملاً](#1)
2. [نوع فاتورة "بيع بدون مخزون" + هامش يدوي](#2)
3. [إصلاح الأرصدة الصفرية لكل الأطراف](#3)
4. [إنشاء endpoint كشف الحساب](#4)
5. [حالة السداد عند إنشاء الفاتورة](#5)
6. [جدول local_invoice_allocations والـ FIFO](#6)
7. [تسوية حالات الهامش (5 أنواع)](#7)
8. [تحسين تبويب الفواتير وفلترة الحالات](#8)
9. [إصلاح حساب paidAmount المزدوج](#9)
10. [إيصال طباعة POS لكل دفعة](#10)
11. [إصلاح خطأ FK عند استلام الفاتورة](#11)
12. [إعادة تصميم قائمة الأطراف](#12)
13. [إصلاح ظهور النواقص في تبويب الهوامش](#13)
14. [إعادة بناء كشف الحساب من المصادر الأولية](#14)
15. [أزرار بطاقية لاختيار نوع الفاتورة](#15)
16. [حالة "مستلم جزئياً" عند وجود نواقص](#16)
17. [إصلاح تسويات الهوامش — العلامة والنوع المرفوض](#17)
18. [تصغير أزرار التعديل والحذف](#18)
19. [تفعيل أزرار نوع الفاتورة بحسب الطرف](#19)
20. [إزالة validation القسمة على 12 (الدستة)](#20)

---

## التحليل التفصيلي لكل تغيير

---

<a name="1"></a>
### 1. توثيق PRD الوحدة كاملاً
**التوقيت**: 11:35 ص — 12:00 م  
**الـ Commits**: `49e45ce`, `f534c8a`, `dc9fa1e`

#### ماذا تم؟
- إنشاء `PRD_التجارة_المحلية.txt` (659 سطراً) يغطي كامل مواصفات الوحدة
- إنشاء `generate-prd.mjs` لتحويله لـ Word (مكتبة `docx`)
- تحديث التوثيق بقسم آلية الهوامش اليدوية والتلقائية

#### لماذا؟
الوحدة كانت مبنية بدون توثيق رسمي للمتطلبات. أي تطوير مستقبلي أو تسليم للمستخدمين يحتاج وثيقة مرجعية واضحة تصف كل workflow من البداية حتى التسوية.

#### الأثر
- مرجع رسمي للوحدة بالكامل
- يُعجّل تدريب أي مستخدم أو مطور جديد
- يُوثّق القرارات التصميمية (مثل FIFO، الهوامش التلقائية) حتى لا تُعاد من الصفر

---

<a name="2"></a>
### 2. نوع فاتورة "بيع بدون مخزون" + هامش يدوي من البروفايل
**التوقيت**: 12:16 م  
**الـ Commits**: `a9565f8`  
**الملفات**: `create-invoice.tsx` (+62 سطر)، `party-profile.tsx` (+131 سطر)

#### ماذا تم؟

**نوع الفاتورة الجديد (`sale_no_stock`)**:
- أُضيف خيار ثالث في منشئ الفاتورة للبيع بدون سحب من المستودع
- مفيد عند بيع بضاعة مباشرة قبل استلامها رسمياً أو عند تسجيل عملية بيع على الورق

**مكوّن `CreatePartyReturnDialog`**:
- نافذة إنشاء هامش يدوي من تبويب الهوامش في البروفايل
- الحقول: وصف، عدد قطع، مبلغ ج.م، الفاتورة المصدر (اختياري)
- زر "هامش يدوي جديد" ظاهر فقط في تبويب الهوامش

#### لماذا؟
الهوامش التلقائية تُنشأ فقط عند الاستلام الجزئي. لكن يوجد كثير من حالات الهامش التي تنشأ خارج عملية الاستلام (بضاعة تالفة بعد الاستلام، خلافات على الجودة، إلخ). المستخدم كان يُضطر لفاتورة كاملة لتسجيل هامش بسيط.

#### الأثر
- المستخدم يُسجّل الهامش من لحظة اكتشافه مباشرة دون إنشاء فاتورة
- `sale_no_stock` يُكمل دورة البيع للتجار الذين يبيعون بالأوامر

---

<a name="3"></a>
### 3. إصلاح الأرصدة الصفرية لكل الأطراف
**التوقيت**: 12:32 م  
**الـ Commits**: `617dd07`  
**الملفات**: `server/storage.ts` (+58 سطر)، `server/routes.ts` (+13 سطر)، `parties.tsx`، `party-profile.tsx`

#### ماذا تم؟

**المشكلة الجذرية**:
```typescript
// الكود القديم — كان يقرأ من حقل غير موجود
currentBalance: party.balance ?? 0  // ❌ 'balance' لا يوجد في الجدول

// الإصلاح — JOIN مع ledger الفعلي
SELECT COALESCE(SUM(ple.amount_egp), 0) AS current_balance
FROM party_ledger_entries ple
WHERE ple.party_id = parties.id
```

- تعديل `getAllParties` ليحسب `currentBalance` بـ SQL JOIN مباشر مع `party_ledger_entries`
- صفحة الأطراف: الرصيد الآن يُحسب كجمع كل قيود الـ ledger (مدين + دائن)

#### لماذا؟
كان كل طرف يظهر رصيداً = 0 دائماً. هذا يجعل صفحة الأطراف عديمة الفائدة تجارياً لأن أهم معلومة (من يدين بكم) كانت مخفية.

#### الأثر
- الرصيد الفعلي يظهر مباشرة عند فتح قائمة الأطراف
- يمكن للمستخدم مباشرة تحديد من عليه ديون ومن يستحق مبالغ

---

<a name="4"></a>
### 4. إنشاء endpoint كشف الحساب
**التوقيت**: 12:32 م  
**الـ Commits**: `617dd07`  
**الملفات**: `server/routes.ts`، `server/storage.ts`، `use-local-trade.ts`

#### ماذا تم؟
- إضافة مسار جديد: `GET /api/local-trade/parties/:id/ledger`
- إضافة `usePartyLedger` hook في الفرونت
- تبويب "كشف الحساب" بدأ يعرض بيانات حقيقية بعد أن كان فارغاً

#### لماذا؟
كشف الحساب كان يعتمد على `partyLedgerEntries` مباشرة — وهذا الجدول لم يكن يُملأ بشكل صحيح في جميع العمليات. المستخدم كان يرى صفحة فارغة أو ناقصة.

#### الأثر
- المستخدم لأول مرة يرى كشف حساب حقيقي لكل طرف
- بنية المسار تسمح بتصفية حسب الموسم مستقبلاً

---

<a name="5"></a>
### 5. حالة السداد عند إنشاء الفاتورة
**التوقيت**: 12:43 م  
**الـ Commits**: `29ed4cc`  
**الملفات**: `create-invoice.tsx` (+120 سطر)، `server/routes.ts` (+21 سطر)، `server/storage.ts`

#### ماذا تم؟

**في الفرونت**:
- قسم "حالة السداد" في نهاية نموذج الفاتورة بـ 3 خيارات:
  - **غير مدفوع** — الافتراضي
  - **مدفوع جزئياً** — يظهر حقل المبلغ + طريقة الدفع
  - **مدفوع بالكامل** — يُملأ المبلغ بمجموع الفاتورة تلقائياً

**في الباك**:
- مسار `POST /api/local-trade/invoices` يقبل `initialPayment` اختياري
- يُنفَّذ atomically: إنشاء الفاتورة + تسجيل الدفعة في transaction واحدة

```typescript
// routes.ts
const { initialPayment, ...invoiceData } = req.body;
const invoice = await storage.createLocalInvoice(invoiceData, lines);
if (initialPayment?.amount > 0) {
  await storage.createLocalPayment({ ...initialPayment, invoiceId: invoice.id });
}
```

#### لماذا؟
الواقع العملي: كثير من المستخدمين يسجّلون الفاتورة والدفع في نفس اللحظة (نقدي عند التسليم). كانوا مضطرين لخطوتين منفصلتين مما يُطيل العملية ويُسبب أخطاء.

#### الأثر
- تسجيل فاتورة + دفعة في خطوة واحدة
- البيانات دائماً متسقة (atomic transaction يمنع حالة "فاتورة بدون دفعة" عند فشل الخطوة الثانية)
- تقليل الضغط على المستخدم في العمليات اليومية

---

<a name="6"></a>
### 6. جدول local_invoice_allocations والـ FIFO
**التوقيت**: 12:58 م  
**الـ Commits**: `31f39f9`  
**الملفات**: `shared/schema.ts` (+29 سطر)، `server/storage.ts` (+138 سطر)، `use-local-trade.ts`

#### ماذا تم؟

**بنية الجدول الجديد**:
```sql
CREATE TABLE local_invoice_allocations (
  id          SERIAL PRIMARY KEY,
  invoice_id  INTEGER NOT NULL REFERENCES local_invoices(id),
  payment_id  INTEGER REFERENCES local_payments(id),       -- إما دفعة
  return_case_id INTEGER REFERENCES return_cases(id),      -- أو تسوية هامش
  amount_egp  NUMERIC NOT NULL,
  source_type TEXT NOT NULL,  -- 'payment' | 'return_deduction'
  created_at  TIMESTAMP DEFAULT NOW()
);
```

**خوارزمية FIFO في `createLocalPayment`**:
```
1. سجّل الدفعة في local_payments
2. سجّل قيد في party_ledger_entries
3. إذا كانت مرتبطة بفاتورة محددة → خصّص كامل المبلغ لها
4. الباقي → وزّعه على أقدم الفواتير غير المسدّدة بالترتيب الزمني (FIFO)
   - لكل فاتورة: احسب (total - allocated_so_far) = outstanding
   - خصّص min(remaining, outstanding)
   - توقف عند نفاد المبلغ أو نفاد الفواتير
```

#### لماذا؟
قبل هذا التغيير، تسجيل دفعة كان يُخزّن مبلغاً إجمالياً فقط بدون أي ربط بالفواتير الفعلية. هذا يعني:
- لا يمكن معرفة أي فاتورة سُدِّدت
- حالة السداد (unpaid/partial/paid) لم تكن دقيقة
- إيصال الدفع لم يستطع عرض الفواتير المرتبطة

#### الأثر التقني
- `paidAmount` لكل فاتورة يُحسب الآن بدقة:
```sql
SELECT COALESCE(SUM(lia.amount_egp), 0)
FROM local_invoice_allocations lia
WHERE lia.invoice_id = :invoiceId
```
- إيصال الدفع يعرض الفواتير التي غطّتها هذه الدفعة بدقة
- كشف الحساب يعرف اتجاه كل دفعة (سداد مبيعات أم مشتريات)

#### الأثر التجاري
- المستخدم يعرف بالضبط كيف يتوزع كل ريال على الفواتير
- يمكن تتبع الدفعات الجزئية عبر الزمن
- أساس متين لأي تقارير أعمار الديون مستقبلاً

---

<a name="7"></a>
### 7. تسوية حالات الهامش (5 أنواع)
**التوقيت**: 12:58 م  
**الـ Commits**: `31f39f9`  
**الملفات**: `server/storage.ts` (+90 سطر)، `server/routes.ts`، `party-profile.tsx` (+290 سطر)

#### ماذا تم؟

**المكوّن الجديد `ResolveReturnCaseDialog`**:
نافذة تسوية الهامش بـ 5 خيارات مع حقول ديناميكية:

| الخيار | المبلغ مطلوب؟ | القطع مطلوبة؟ | أثر الـ Ledger | أثر الفاتورة |
|---|---|---|---|---|
| **خصم قيمة** | ✅ نعم | ✅ نعم | ±amountEgp حسب نوع الطرف | ينقص paidAmount |
| **إرجاع مقبول** | ✅ نعم | ✅ نعم | ±amountEgp حسب نوع الطرف | ينقص paidAmount |
| **استبدال** | ❌ لا | ✅ نعم | صفر | لا أثر |
| **شطب تالف** | ✅ نعم | ✅ نعم | صفر | لا أثر |
| **مرفوض** | ❌ لا | ❌ لا | صفر | لا أثر |

**الـ Hook `useResolveReturnCase`**:
بعد نجاح التسوية يُبطل 4 caches دفعة واحدة:
1. `return-cases` (تبويب الهوامش)
2. `invoices-with-payments` (تبويب الفواتير)
3. `local-payments` (تبويب المدفوعات)
4. `parties` + `party-ledger` (الرصيد + كشف الحساب)

#### لماذا؟
الهوامش كانت تُنشأ وتبقى "تحت الفحص" بدون أي آلية لإغلاقها. هذا يجعل التتبع المالي ناقصاً ويُراكم بنوداً معلقة.

#### الأثر التقني
- `resolveReturnCase` يعمل كـ atomic transaction واحدة:
  1. تحديث حالة الهامش → `resolved`
  2. قيد في `party_ledger_entries`
  3. إدخال في `local_invoice_allocations` (عند خصم أو إرجاع)
- لا يمكن أن ينجح جزء ويفشل آخر

#### الأثر التجاري
- دورة حياة الهامش مكتملة: نشأ ← تحت الفحص ← محلول
- الرصيد يتحدث فوراً بعد التسوية
- الفاتورة المصدر تتحدث (المبلغ المدفوع يزداد بقيمة الخصم/الإرجاع)

---

<a name="8"></a>
### 8. تحسين تبويب الفواتير وفلترة الحالات
**التوقيت**: 1:09 م  
**الـ Commits**: `84849c5`  
**الملفات**: `party-profile.tsx` (+366 سطر)، `server/storage.ts`

#### ماذا تم؟

**`InvoicesTab` — الفلترة الجديدة**:
6 خيارات فلترة: الكل / مسودة / معلقة / مستلمة جزئياً / مستلمة / ملغاة

**كل صف فاتورة يعرض**:
- أيقونة نوع الفاتورة (📦 شراء / 🛒 بيع / ⚡ بدون مخزون)
- الإجمالي / المدفوع / المتبقي (3 أرقام واضحة)
- badge لون حالة السداد: أحمر=غير مدفوع، أصفر=جزئي، أخضر=مكتمل
- زر "استلام" يظهر فقط للفواتير في حالة `pending`

**إصلاح فلتر FIFO**:
- FIFO يتجاهل الفواتير في حالة `cancelled` و `archived`
- يتجاهل الفواتير المسدّدة بالكامل (outstanding ≈ 0)

#### لماذا؟
قبل الفلترة، المستخدم يرى قائمة طويلة مختلطة من مراحل مختلفة ولا يعرف أين يبدأ. الفاتورة الجاهزة للاستلام تختلط بالملغاة والمكتملة.

#### الأثر
- وضوح فوري عن حالة كل فاتورة
- المستخدم يفلتر "معلقة" ويرى فقط ما يحتاج عمل
- تقليل الأخطاء (مثل محاولة استلام فاتورة مستلمة مسبقاً)

---

<a name="9"></a>
### 9. إصلاح حساب paidAmount المزدوج
**التوقيت**: 1:16 م  
**الـ Commits**: `26b455a`  
**الملفات**: `server/storage.ts` (+92 سطر)

#### ماذا تم؟

**المشكلة**:
عند إضافة `local_invoice_allocations`، أصبح هناك مصدران لـ `paidAmount`:
1. سجلات في `local_payments` مرتبطة بـ `invoiceId` مباشرة (الكود القديم)
2. سجلات في `local_invoice_allocations` (الكود الجديد)

بعض الدفعات تظهر في كلا المصدرين → ازدواجية في الحساب.

**الإصلاح بـ SQL Anti-JOIN**:
```sql
paidAmount = 
  -- المصدر 1: allocations (الجديد)
  SELECT COALESCE(SUM(lia.amount_egp), 0)
  FROM local_invoice_allocations lia
  WHERE lia.invoice_id = invoice.id

  + 

  -- المصدر 2: الدفعات المباشرة القديمة التي لا يوجد لها allocation
  SELECT COALESCE(SUM(lp.amount_egp), 0)
  FROM local_payments lp
  WHERE lp.invoice_id = invoice.id
  AND NOT EXISTS (
    SELECT 1 FROM local_invoice_allocations lia2
    WHERE lia2.payment_id = lp.id AND lia2.invoice_id = invoice.id
  )
```

#### لماذا؟
بدون هذا الإصلاح، الفاتورة التي قيمتها 1000 ج.م وسُدِّدت 1000 ج.م قد تظهر `paidAmount = 2000` مما يجعل `remainingAmount = -1000` — وهو رقم خاطئ.

#### الأثر
- `paymentStatus` (unpaid/partial/paid) دقيق 100%
- `remainingAmount` دائماً ≥ 0
- التوافق مع الدفعات القديمة (قبل إضافة الـ allocations)

---

<a name="10"></a>
### 10. إيصال طباعة POS لكل دفعة
**التوقيت**: 1:32 م  
**الـ Commits**: `fc38f5c`  
**الملفات**: `party-profile.tsx` (+307 سطر)

#### ماذا تم؟

**مكوّن `PaymentReceiptDialog`**:
نافذة كاملة تعرض إيصال الدفعة:
- اسم الطرف ونوعه
- تاريخ الدفعة وطريقتها ورقم المرجع
- جدول الفواتير المُسوَّاة (من FIFO allocations): رقم الفاتورة + المبلغ المُسوَّى
- ملخص: إجمالي مستحق / مدفوع اليوم / الرصيد المتبقي

**زر الطباعة**:
```typescript
// يفتح نافذة طباعة بـ CSS مخصص 80mm
const printWindow = window.open('', '_blank', 'width=320,height=600');
printWindow.document.write(`
  <style>
    @page { margin: 2mm; }
    body { font-family: 'Cairo', sans-serif; direction: rtl; font-size: 10px; }
  </style>
  ${receiptHTML}
`);
printWindow.print();
```

#### لماذا؟
التجار يحتاجون إيصالاً فيزيائياً للمستخدم أو للمحاسبة. طابعات POS (80mm) منتشرة جداً في البيئة التجارية المصرية.

#### الأثر
- توثيق فيزيائي لكل دفعة
- الإيصال يعرض تفاصيل التوزيع على الفواتير (مزية غير موجودة في أنظمة كثيرة)
- CSS مخصص للطباعة يمنع ظهور عناصر الواجهة الزائدة

---

<a name="11"></a>
### 11. إصلاح خطأ FK عند استلام الفاتورة
**التوقيت**: 1:51 م  
**الـ Commits**: `1e143d7`  
**الملفات**: `server/storage.ts` (−11 سطر)

#### ماذا تم؟
حذف الكود الذي كان يحاول إنشاء سجل في `inventory_movements`:

```typescript
// ❌ الكود المحذوف — كان يُسبب FK violation
await this.createInventoryMovement({
  shipmentItemId: line.shipmentItemId,  // NULL في التجارة المحلية
  productId: line.productId,            // FK يشير لجدول products من الشحنات
  ...
});
```

**السبب الجذري**: `inventory_movements` مرتبط بـ `products` (جدول وحدة الشحنات)، بينما التجارة المحلية تستخدم `product_types` منفصلاً. الربط كان مستحيلاً.

**الإصلاح**: التجارة المحلية تتبع الكميات عبر `receivedPieces` في `local_invoice_lines` مباشرة — لا حاجة لـ `inventory_movements`.

#### لماذا؟
هذا الخطأ كان يمنع استلام أي فاتورة نهائياً. الزر "استلام" كان يُعطي `500 Internal Server Error` دائماً.

#### الأثر
- استلام الفواتير يعمل بشكل كامل
- نُظّفت العلاقة بين وحدة الشحنات والتجارة المحلية — كل وحدة مستقلة
- −11 سطر بدون أي خسارة وظيفية

---

<a name="12"></a>
### 12. إعادة تصميم قائمة الأطراف
**التوقيت**: 1:57 م  
**الـ Commits**: `afb9cdd`  
**الملفات**: `parties.tsx` (−246 +173 = إعادة كتابة جوهرية)

#### ماذا تم؟
**من**: جدول تقليدي بـ 10 أعمدة مزدحمة  
**إلى**: بطاقات صفية واسعة مريحة

**المزايا الجديدة**:
- **أفاتار ملوّن**: الحرف الأول من الاسم، اللون يعكس نوع الطرف
- **الضغط في أي مكان** من الصف ينتقل لبروفايل الطرف
- **عرض مدمج**: الاسم + المحل + الهاتف + العنوان في سطر منظّم
- **الرصيد بارز**: أحمر = "عليه" (يدين لنا) / أخضر = "له" (ندين له)
- **زر تعديل** يظهر فقط عند hover — لا يُعيق الضغط للانتقال
- **Badge نوع الطرف**: تاجر (أزرق) / عميل (أخضر) / مزدوج (بنفسجي)

#### لماذا؟
التصميم القديم كان يُظهر 10 أعمدة في جدول ضيق مما يعني:
- قطع الأسماء الطويلة
- صعوبة القراءة على الشاشات الصغيرة
- المستخدم يقرأ بأفقي بدلاً من عمودي (أبطأ للعين)

#### الأثر
- سرعة أكبر في تحديد الطرف المطلوب
- معلومات الاتصال والرصيد ظاهرة دون الدخول للبروفايل
- تجربة مستخدم أكثر احترافية

---

<a name="13"></a>
### 13. إصلاح ظهور النواقص في تبويب الهوامش
**التوقيت**: 2:02 م  
**الـ Commits**: `5ab815d`  
**الملفات**: `use-local-trade.ts` (+2 سطر)، `party-profile.tsx` (+54 سطر)، `server/storage.ts` (+6 سطر)

#### ماذا تم؟

**المشكلة أ — النواقص لا تظهر في الهوامش بعد الاستلام**:
```typescript
// ❌ قبل — يُحدّث cache الفواتير فقط
useReceiveInvoice: {
  onSuccess: () => {
    queryClient.invalidateQueries(['invoices-with-payments'])
  }
}

// ✅ بعد — يُحدّث 3 caches
useReceiveInvoice: {
  onSuccess: () => {
    queryClient.invalidateQueries(['invoices-with-payments'])
    queryClient.invalidateQueries(['return-cases'])     // ← جديد
    queryClient.invalidateQueries(['parties'])          // ← جديد
  }
}
```

**المشكلة ب — `partyTypeSnapshot` = 'merchant' دائماً**:
```typescript
// ❌ قبل — hardcoded
await this.createReturnCase({ partyTypeSnapshot: 'merchant', ... });

// ✅ بعد — يجلب من DB
const partyData = await this.getParty(invoice.partyId);
const partyTypeSnapshot = partyData?.type || 'merchant';
```

**المشكلة ج — مبلغ الهامش مخفي قبل التسوية**:
```tsx
// ❌ قبل — يظهر فقط إذا resolved
{rc.status === 'resolved' && rc.amountEgp && <span>{rc.amountEgp}</span>}

// ✅ بعد — يظهر دائماً مع لون مختلف
<span className={rc.status === 'resolved' ? 'text-green-600' : 'text-amber-600'}>
  {rc.amountEgp}
</span>
```

#### لماذا؟
المستخدم يستلم فاتورة فيها نواقص، ثم ينتقل لتبويب الهوامش — فيجده فارغاً. هذا يُربك المستخدم ويجعله يعتقد أن النظام لم يُسجّل النواقص.

#### الأثر
- الانتقال من الاستلام للهوامش يعطي feedback فورياً
- `partyTypeSnapshot` الصحيح يضمن حسابات رصيد دقيقة لاحقاً
- المبلغ الظاهر يساعد المستخدم في قرار التسوية قبل فتح النافذة

---

<a name="14"></a>
### 14. إعادة بناء كشف الحساب من المصادر الأولية
**التوقيت**: 2:15 م  
**الـ Commits**: `3720368`  
**الملفات**: `server/storage.ts` (+250 سطر)، `party-profile.tsx` (+180 سطر)

#### ماذا تم؟

**المشكلة الجذرية**:
كشف الحساب كان يقرأ فقط من `party_ledger_entries` — جدول يُملأ يدوياً عند كل عملية. المشكلة: بعض العمليات لم تكن تُضيف قيوداً صحيحة، فكان الكشف ناقصاً أو مغلوطاً.

**الحل الجديد — البناء من المصادر الأولية**:
```
getPartyLedgerEntries() الآن يفعل:

1. يجلب كل الفواتير (local_invoices) للطرف مرتبة زمنياً
2. يجلب كل المدفوعات (local_payments) للطرف مرتبة زمنياً
3. يجلب القيود اليدوية فقط (رصيد افتتاحي، تسويات، تعديلات)
4. للمدفوعات: يُحدد اتجاه كل دفعة عبر JOIN على local_invoice_allocations
5. يدمج الثلاثة في قائمة مرتبة زمنياً ويحسب الرصيد المتراكم
```

**اتفاقية العلامات الموحّدة**:
```
موجب (+) = مدين = هم يدينون لنا
سالب (−) = دائن = نحن ندين لهم

فاتورة بيع (sale):   +totalEgp  ← هم اشتروا منا → يدينون لنا
فاتورة شراء (purchase): −totalEgp  ← نحن اشترينا منهم → ندين لهم
دفعة على بيع:      −payAmt    ← هم دفعوا لنا → يُنقص ما عليهم
دفعة على شراء:     +payAmt    ← نحن دفعنا لهم → يُنقص ما علينا
```

**جدول كشف الحساب في الواجهة**:

| التاريخ | النوع (badge) | البيان | مدين (لنا) | دائن (علينا) | الرصيد |
|---|---|---|---|---|---|
| خلفية زرقاء للشراء، خضراء للبيع، بنفسجية للدفعة، عنبرية للهامش |

- شريط رصيد لاصق في الأسفل يعرض الرصيد الإجمالي مع اتجاهه
- تصدير PDF وCSV يشمل عمود "النوع"

#### لماذا؟
كان كشف الحساب ذا مصداقية منخفضة لأن أي عملية لا تُسجّل قيدها بشكل صحيح تُختفي من الكشف. المصادر الأولية (الفواتير والمدفوعات أنفسهم) هي الحقيقة الوحيدة.

#### الأثر التقني
- كشف الحساب مبني على نفس البيانات التي تبني عليها الفواتير والمدفوعات — لا يمكن أن يختلف
- `Promise.all` يجلب الثلاثة مصادر بالتوازي → أداء أفضل

#### الأثر التجاري
- المستخدم يرى كل لحظة مالية في تاريخ الطرف
- الرصيد المتراكم في كل سطر يُجيب على "كيف وصلنا لهذا الرصيد"
- ألوان الخلفية تُسهّل التمييز البصري الفوري

---

<a name="15"></a>
### 15. أزرار بطاقية لاختيار نوع الفاتورة
**التوقيت**: 2:30 م  
**الـ Commits**: `0fa12ec`  
**الملفات**: `create-invoice.tsx`

#### ماذا تم؟
**من**: قائمة منسدلة `<Select>` بسيطة  
**إلى**: 3 أزرار بطاقية بصرية كبيرة

```
┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│  📦 شراء   │  │  🛒  بيع   │  │ ⚡ بدون    │
│ (أزرق)     │  │ (أخضر)     │  │  مخزون     │
│             │  │             │  │ (برتقالي)  │
│     ●       │  │             │  │             │
└─────────────┘  └─────────────┘  └─────────────┘
```

- البطاقة المحددة: حدود ملوّنة + نقطة تأكيد + خلفية فاتحة
- البطاقة المعطّلة: `opacity-50 cursor-not-allowed`

#### لماذا؟
القائمة المنسدلة لنوع الفاتورة كانت تُجبر المستخدم على نقرتين (فتح القائمة + الاختيار). الأنواع الثلاثة فقط تستحق عرضاً بارزاً مع تمييز بصري واضح — خصوصاً أن الاختيار يُغير كل flow الفاتورة.

#### الأثر
- الاختيار بنقرة واحدة
- النوع المحدد واضح بصرياً دون الحاجة لقراءة نص
- يُقلل من أخطاء "نسيت تغيير النوع"

---

<a name="16"></a>
### 16. حالة "مستلم جزئياً" عند وجود نواقص
**التوقيت**: 2:45 م  
**الـ Commits**: `0fa12ec`  
**الملفات**: `server/storage.ts`

#### ماذا تم؟
```typescript
// ❌ قبل — دائماً 'received' حتى لو فيها نواقص
await this.updateLocalInvoice(invoiceId, { status: 'received' });

// ✅ بعد — يُفرّق بين مكتمل وجزئي
const finalStatus = marginsCreated > 0 ? 'partially_received' : 'received';
await this.updateLocalInvoice(invoiceId, { status: finalStatus });
```

#### لماذا؟
فاتورة فيها 100 قطعة مستلمة 80 منها كانت تُظهر "مستلمة" — وهذا كاذب. المحاسب الذي يبحث عن الفواتير الناقصة لم يكن يجدها.

#### الأثر
- الفلترة بـ "مستلمة جزئياً" تعطي قائمة دقيقة بالفواتير التي تحتاج متابعة
- حالة الفاتورة تعكس الواقع الحقيقي
- أساس لمنطق إغلاق تلقائي مستقبلاً (عند حل كل هوامشها)

---

<a name="17"></a>
### 17. إصلاح تسويات الهوامش — العلامة والنوع المرفوض
**التوقيت**: 2:58 م  
**الـ Commits**: `38677ce`  
**الملفات**: `server/routes.ts`، `server/storage.ts`، `use-local-trade.ts`، `party-profile.tsx`

#### ماذا تم؟

**المشكلة 1 — `مرفوض` يُعطي 400**:
```typescript
// ❌ قبل
const validResolutions = ['accepted_return', 'exchange', 'deduct_value', 'damaged'];
// 'rejected' غير موجود → throw 400

// ✅ بعد
const validResolutions = ['accepted_return', 'exchange', 'deduct_value', 'damaged', 'rejected'];
```

**المشكلة 2 — علامة خاطئة للتاجر**:

التاجر = مورّد (نحن نشتري منه):
- إذا أرجع بضاعة → نحن ندين له أقل → الرصيد يُصبح أقل سالباً → نضيف موجب
- إذا خُصم منه قيمة → نفس المنطق

```typescript
// اتفاقية: موجب = يدينون لنا، سالب = ندين لهم
const isMerchant = partyType === 'merchant';

// accepted_return / deduct_value
ledgerAmount = isMerchant ? +amountEgp : -amountEgp;
//              ↑ تاجر: يُقلل ما ندين له   ↑ عميل: يُقلل ما علينا تحصيله
```

**المشكلة 3 — كشف الحساب لا يتحدث بعد التسوية**:
```typescript
// ❌ قبل — invalidateQueries كانت تعتمد على partyId في الـ queryKey
queryClient.invalidateQueries(['party-ledger', partyId])

// ✅ بعد — predicate يُبطل أي query key يبدأ بـ 'party-ledger'
queryClient.invalidateQueries({
  predicate: (query) => query.queryKey[0] === 'party-ledger'
})
```

#### لماذا؟
- `مرفوض` حالة شائعة جداً (التاجر يرفض الادعاء) وكانت تُعطي خطأً
- العلامة الخاطئة كانت تُظهر رصيداً معاكساً بعد التسوية — مشكلة محاسبية خطيرة
- عدم تحديث الكشف كان يُوهم المستخدم أن التسوية لم تُنفَّذ

#### الأثر
- الخمسة أنواع كلها تعمل بشكل صحيح الآن
- الرصيد والكشف يتحدثان فوراً بعد التسوية
- المحاسب يرى الأثر الصحيح لكل نوع تسوية

---

<a name="18"></a>
### 18. تصغير أزرار التعديل والحذف
**التوقيت**: 3:00 م  
**الـ Commits**: `e1a5b7a`  
**الملفات**: `party-profile.tsx`

#### ماذا تم؟
**من**: أزرار نصية كبيرة `[تعديل] [حذف]`  
**إلى**: أيقونات `✏️` و `🗑️` بحجم 32×32 بكسل مع Tooltip

```tsx
// بدلاً من
<Button variant="outline">تعديل</Button>
<Button variant="destructive">حذف</Button>

// يصبح
<Button variant="ghost" size="icon" className="h-8 w-8">
  <Pencil className="h-4 w-4" />
</Button>
<Button variant="ghost" size="icon" 
  className="h-8 w-8 border border-destructive/30 hover:bg-destructive hover:text-white">
  <Trash2 className="h-4 w-4" />
</Button>
```

#### لماذا؟
في بروفايل الطرف، الأزرار النصية الكبيرة كانت تستهلك مساحة في صفوف القوائم مما يُقلل عدد العناصر الظاهرة دون التمرير. التصميم الأيقوني أكثر احترافية وأقل ازدحاماً.

#### الأثر
- كثافة معلومات أعلى في نفس المساحة
- زر الحذف الأحمر عند hover يُقلل الحذف الخاطئ (المستخدم يلاحظ خطورته)

---

<a name="19"></a>
### 19. تفعيل أزرار نوع الفاتورة بحسب الطرف
**التوقيت**: 3:05 م  
**الـ Commits**: `77c527a`  
**الملفات**: `create-invoice.tsx`

#### ماذا تم؟

**المشكلة**: عند الدخول لمنشئ الفاتورة من بروفايل طرف محدد، كانت أزرار النوع الثلاثة معطّلة كلها:
```typescript
// ❌ قبل — كل الأزرار معطّلة عند تحديد طرف مسبقاً
const isDisabled = !!initialPartyId;
```

**الإصلاح — منطق ذكي بحسب نوع الطرف**:
```typescript
// ✅ بعد
const allowedTypes = useMemo(() => {
  if (!selectedParty) return ['purchase', 'sale', 'sale_no_stock'];
  if (selectedParty.type === 'merchant') return ['purchase'];
  if (selectedParty.type === 'customer') return ['sale', 'sale_no_stock'];
  return ['purchase', 'sale', 'sale_no_stock']; // 'both'
}, [selectedParty]);

// كل زر
const isDisabled = !allowedTypes.includes(type);
```

#### لماذا؟
- التاجر (مورّد): من المنطقي فقط الشراء منه
- العميل: من المنطقي فقط البيع له
- مزدوج: كل الخيارات

الكود القديم كان يُعطّل كل الأزرار بحجة "الطرف محدد مسبقاً" — منطق غير صحيح.

#### الأثر
- الإنشاء من البروفايل يعمل بشكل طبيعي
- قواعد العمل مُطبَّقة تلقائياً (لا يمكن بيع لتاجر أو شراء من عميل)
- تقليل أخطاء اختيار النوع الخاطئ

---

<a name="20"></a>
### 20. إزالة validation القسمة على 12 (الدستة)
**التوقيت**: 3:10 م  
**الـ Commits**: `603b8c6`, `1d09562`, `a74e568`  
**الملفات**: `server/storage.ts`، `server/routes.ts`، `party-profile.tsx`، `invoices.tsx`

#### ماذا تم؟

**الـ validation المحذوف من 4 أماكن**:
```typescript
// ❌ الكود المحذوف (نفس النمط في كل الأماكن)
function validateDozenQuantity(quantity: number, unit: string): string | null {
  if (unit === 'dozen' && quantity % 12 !== 0) {
    return `الكمية يجب أن تكون مضاعفاً لـ 12 عند استخدام الدستة`;
  }
  return null;
}

// في الفرونت — زر الحفظ كان معطّلاً
disabled={hasDozenValidationErrors || isLoading || ...}
```

**أربعة أماكن تم تعديلها**:
1. `server/storage.ts` — `createLocalInvoice` لم يعد يرفض الكميات
2. `server/routes.ts` — مسار POST لم يعد يُعيد 400
3. `party-profile.tsx` — حذف `validateDozenQuantity` + `hasDozenValidationErrors` بالكامل
4. `invoices.tsx` — نفس الحذف + إزالة آخر `hasDozenValidationErrors` من disabled

**العرض الجديد — دستات كسرية**:
```tsx
// ❌ قبل — يُخفي الدستات إذا غير قابلة للقسمة
{quantity % 12 === 0 && <span>{quantity / 12} دستة</span>}

// ✅ بعد — يعرض دائماً مع كسور عشرية
{unit === 'dozen' && quantity > 0 && (
  <span className="text-xs text-muted-foreground">
    {(quantity / 12).toFixed(2)} دستة
  </span>
)}
```

#### لماذا؟
الواقع التجاري: المورد يبيع 575 قطعة — وهذا ليس مضاعفاً لـ 12. إلزام المستخدم بالقسمة على 12 كان:
- يمنعه من تسجيل العمليات الحقيقية
- يُجبره على "تقريب" الكمية (يُفسد دقة البيانات)
- يُسبب إحباطاً لأن النظام يُعطي خطأً لا يفهم سببه

**الرياضيات صحيحة دائماً**:
```
575 قطعة × (12 ج.م / دستة) = 575 ÷ 12 × 12 = 575 ج.م
```
أي: السعر بالدستة × (الكمية ÷ 12) = إجمالي صحيح، بغض النظر عن القسمة.

#### الأثر
- المستخدم يُسجّل أي كمية حقيقية
- البيانات أدق وتعكس الواقع
- إزالة مصدر إحباط رئيسي في سير العمل اليومي

---

## تحليل الأنماط والقرارات المعمارية

### نمط 1: Atomic Transactions في كل عملية مُركّبة
كل عملية تُعدّل أكثر من جدول تعمل داخل `db.transaction()`:
- `createLocalInvoice` → invoice + ledger entry
- `createLocalPayment` → payment + ledger entry + allocations (FIFO)
- `resolveReturnCase` → update case + ledger entry + allocation
- `createInvoice` مع `initialPayment` → invoice + lines + payment + allocation

**الهدف**: لا يمكن أن تنجح نصف العملية. إما الكل أو لا شيء.

---

### نمط 2: Cache Invalidation الشاملة
كل mutation بعد نجاحها تُبطل كل الـ caches المرتبطة:
```typescript
// مثال: بعد تسوية هامش
queryClient.invalidateQueries(['return-cases'])           // الهوامش
queryClient.invalidateQueries(['invoices-with-payments']) // الفواتير
queryClient.invalidateQueries(['local-payments'])         // المدفوعات
queryClient.invalidateQueries(['parties'])               // الأرصدة
queryClient.invalidateQueries({ predicate: q => q.queryKey[0] === 'party-ledger' })
```

**الهدف**: لا يظل أي جزء من الواجهة يعرض بيانات قديمة بعد أي تغيير.

---

### نمط 3: اتفاقية العلامات الموحّدة
```
موجب (+) = مدين = هم يدينون لنا (receivable)
سالب (−) = دائن = نحن ندين لهم (payable)
```

هذه الاتفاقية مُطبَّقة بشكل متسق في:
- `createLocalInvoice` (ledger entry)
- `createLocalPayment` (ledger entry)
- `resolveReturnCase` (ledger entry بحسب `partyTypeSnapshot`)
- `getPartyLedgerEntries` (الكشف النهائي)

---

### نمط 4: Backward Compatibility في paidAmount
الـ SQL Anti-JOIN في `getAllLocalInvoicesWithPayments` يضمن:
- الدفعات القديمة (مرتبطة بـ `invoiceId` مباشرة، بدون allocation) تُحسب
- الدفعات الجديدة (عبر allocations) تُحسب
- لا ازدواجية

---

## ملخص الملفات المعدّلة

| الملف | قبل (تقدير) | بعد | التغيير الرئيسي |
|---|---|---|---|
| `server/storage.ts` | ~4,500 | 4,921 | FIFO، كشف الحساب، تسويات، استلام |
| `server/routes.ts` | ~3,200 | 3,291 | تسويات، validation، initialPayment |
| `party-profile.tsx` | ~2,200 | 3,778 | FIFO badges، POS، استلام، هوامش، أزرار |
| `create-invoice.tsx` | ~650 | 887 | بطاقات النوع، حالة السداد، allowedTypes |
| `parties.tsx` | ~550 | 545 | إعادة تصميم كاملة |
| `invoices.tsx` | ~1,200 | 1,186 | إزالة dozen validation |
| `shared/schema.ts` | ~808 | 837 | جدول `local_invoice_allocations` |
| `use-local-trade.ts` | ~415 | 439 | hooks جديدة، invalidation |

---

## الـ Bugs الحرجة المُصلَحة اليوم

| # | الخطورة | المشكلة | السبب الجذري | الإصلاح |
|---|---|---|---|---|
| 1 | 🔴 حرج | رصيد الطرف = 0 دائماً | قراءة من حقل غير موجود | JOIN مع ledger الفعلي |
| 2 | 🔴 حرج | FK error عند استلام أي فاتورة | استدعاء inventory_movements من وحدة شحنات | حذف الاستدعاء |
| 3 | 🔴 حرج | `مرفوض` يُعطي 500 | غياب من validResolutions | إضافته |
| 4 | 🟠 عالٍ | علامة رصيد خاطئة للتاجر بعد التسوية | منطق خاطئ في isMerchant | إصلاح الإشارة |
| 5 | 🟠 عالٍ | paidAmount مضاعف للدفعات القديمة | مصدران يحسبان نفس الدفعة | Anti-JOIN SQL |
| 6 | 🟠 عالٍ | كشف الحساب فارغ/ناقص | اعتماد على ledger غير مكتمل | بناء من المصادر الأولية |
| 7 | 🟡 متوسط | النواقص لا تظهر فوراً في الهوامش | invalidation ناقصة | إضافة return-cases invalidation |
| 8 | 🟡 متوسط | `partyTypeSnapshot` = 'merchant' دائماً | hardcoded | جلب من DB |
| 9 | 🟡 متوسط | أزرار نوع الفاتورة معطّلة من البروفايل | `isDisabled = !!initialPartyId` خاطئ | منطق بحسب نوع الطرف |
| 10 | 🟡 متوسط | فاتورة 575 دستة مرفوضة | validation القسمة على 12 | حذف الـ validation |

---

## الميزات المكتملة اليوم

| # | الميزة | التعقيد | الوقت |
|---|---|---|---|
| 1 | توثيق PRD رسمي بـ Word | منخفض | 25 دقيقة |
| 2 | نوع بيع بدون مخزون + هامش يدوي | متوسط | 16 دقيقة |
| 3 | إصلاح أرصدة الأطراف | متوسط | 16 دقيقة |
| 4 | كشف حساب API + hook | متوسط | جزء من #3 |
| 5 | حالة السداد عند إنشاء الفاتورة | متوسط | 11 دقيقة |
| 6 | FIFO للمدفوعات + جدول allocations | **عالٍ** | 15 دقيقة |
| 7 | تسوية حالات الهامش 5 أنواع | **عالٍ** | جزء من #6 |
| 8 | فلترة الفواتير بالحالة | منخفض | 11 دقيقة |
| 9 | إصلاح paidAmount Anti-JOIN | متوسط | 7 دقيقة |
| 10 | إيصال طباعة POS | متوسط | 16 دقيقة |
| 11 | إصلاح FK الاستلام | منخفض | فوري |
| 12 | إعادة تصميم قائمة الأطراف | متوسط | 6 دقيقة |
| 13 | إصلاح ظهور النواقص | منخفض | 5 دقيقة |
| 14 | إعادة بناء كشف الحساب | **عالٍ** | 13 دقيقة |
| 15 | أزرار بطاقية لنوع الفاتورة | منخفض | 15 دقيقة |
| 16 | حالة partially_received | منخفض | جزء من #15 |
| 17 | إصلاح تسويات الهوامش | متوسط | 13 دقيقة |
| 18 | تصغير أزرار التعديل/الحذف | منخفض | 2 دقيقة |
| 19 | allowedTypes لنوع الفاتورة | منخفض | 5 دقيقة |
| 20 | إزالة dozen validation | منخفض | 5 دقيقة |

---

## حالة الوحدة في نهاية اليوم

### ما يعمل بشكل كامل ✅
- إنشاء الأطراف (تاجر / عميل / مزدوج)
- إنشاء الفواتير (شراء / بيع / بيع بدون مخزون) مع أو بدون دفعة مبدئية
- استلام الفواتير مع تحديد الكميات المستلمة لكل بند
- إنشاء هوامش تلقائية عند الاستلام الجزئي
- إنشاء هوامش يدوية من البروفايل
- تسوية الهوامش بالأنواع الخمسة مع أثر صحيح على الرصيد والفاتورة
- تسجيل المدفوعات مع FIFO allocation تلقائي
- طباعة إيصالات POS
- كشف الحساب مبني من المصادر الأولية
- أرصدة الأطراف دقيقة وتتحدث فوراً

### ما يحتاج تطوير مستقبلي 📋
- تقرير أعمار الديون (aging report)
- إشعارات الديون المتأخرة (جزء من notifications موجود)
- أرشفة الفواتير والأطراف
- تقرير الموسم (مقارنة بين مواسم)
- استيراد/تصدير بيانات الأطراف

---

*تقرير مُحرَّر يدوياً — Tracker System — 2026-05-02*  
*إجمالي ساعات التطوير: ~3.7 ساعة*
