# تقرير التطوير اليومي (PDR)
## نظام Tracker — وحدة التجارة المحلية
### تاريخ: السبت 2 مايو 2026

---

## ملخص تنفيذي

| البند | التفاصيل |
|---|---|
| **مدة الجلسة** | 11:35 ص — 2:02 م (2 ساعة و27 دقيقة) |
| **عدد الـ Commits** | 13 commit |
| **الملفات المُعدَّلة** | 13 ملف |
| **إجمالي الأسطر المضافة** | +2,752 سطر |
| **إجمالي الأسطر المحذوفة** | −393 سطر |
| **الوحدة المستهدفة** | التجارة المحلية (Local Trade Module) |

---

## تفاصيل التعديلات (مرتبة زمنياً)

---

### 1. إنشاء وثيقة PRD للوحدة (11:35 ص)
**الهدف**: توثيق متطلبات وحدة إدارة الأطراف  
**ما تم**:
- إنشاء ملف `PRD_التجارة_المحلية.txt` (659 سطراً) يشمل كامل مواصفات الوحدة
- توثيق: إدارة الأطراف، الفواتير، المدفوعات، الهوامش، كشف الحساب، التحصيل، الأرشيف
- تغطية كاملة لـ workflow كل عملية من إنشاء حتى التسوية

**الملفات**:
- `PRD_التجارة_المحلية.txt` ← جديد

---

### 2. إنشاء ملف Word من الـ PRD (11:41 ص)
**الهدف**: تحويل التوثيق لصيغة Word قابلة للتوزيع  
**ما تم**:
- تثبيت مكتبة `docx` لإنشاء ملفات Word برمجياً
- كتابة سكريبت `generate-prd.mjs` (778 سطراً) يُنشئ ملف `.docx` منسّق
- تضمين: جداول، عناوين هرمية، ترقيم، تنسيق احترافي باللغة العربية

**الملفات**:
- `generate-prd.mjs` ← جديد
- `PRD_التجارة_المحلية.docx` ← جديد
- `package.json` ← إضافة `docx`

---

### 3. تحديث توثيق الهوامش في الـ PRD (11:48 ص)
**الهدف**: إضافة توثيق تفصيلي لآلية الهوامش التلقائية واليدوية  
**ما تم**:
- توثيق إنشاء الهوامش التلقائية عند استلام الفاتورة بكمية ناقصة
- توثيق إنشاء الهوامش اليدوية من بروفايل الطرف
- توثيق خيارات التسوية الخمسة (خصم / مرتجع / استبدال / تالف / مرفوض)

---

### 4. إضافة نوع "بيع بدون مخزون" + هامش يدوي (12:16 م)
**الهدف**: توسيع أنواع الفواتير وإضافة هوامش يدوية من البروفايل  
**ما تم**:

**الفرونت (`create-invoice.tsx`)**:
- إضافة نوع فاتورة جديد `sale_no_stock` (بيع بدون مخزون من المستودع)
- قفل اختيار الطرف تلقائياً عند الدخول من بروفايل طرف محدد

**الفرونت (`party-profile.tsx`)**:
- إضافة مكوّن `CreatePartyReturnDialog`: نافذة لإنشاء هامش يدوي من بروفايل الطرف
- حقول: وصف الهامش، عدد القطع، المبلغ، الفاتورة المصدر (اختياري)
- زر "هامش يدوي جديد" في تبويب الهوامش

**الملفات المُعدَّلة**:
- `client/src/pages/local-trade/create-invoice.tsx` (+62 سطر)
- `client/src/pages/local-trade/party-profile.tsx` (+131 سطر)

---

### 5. إصلاح أرصدة الأطراف وعرض كشف الحساب (12:32 م)
**الهدف**: إصلاح الأرصدة التي كانت تظهر صفراً دائماً  
**المشاكل التي تم إصلاحها**:
- `currentBalance` في البروفايل كان يقرأ من حقل غير موجود → أصبح يقرأ من الـ ledger الفعلي
- صفحة الأطراف كانت تُظهر رصيد صفر لكل الأطراف

**ما تم**:

**الباك (`server/routes.ts`, `storage.ts`)**:
- إضافة endpoint جديد `/api/local-trade/parties/:id/ledger` يُعيد كامل قيود الحساب مع الرصيد الجاري
- تعديل `getAllParties` لحساب `currentBalance` فعلي من جدول `partyLedgerEntries` عبر JOIN
- الأرصدة الآن: تجمع كل القيود (مدين/دائن) وتعرض الاتجاه (عليه/له)

**الفرونت**:
- إضافة hook `usePartyLedger` يستدعي الـ endpoint الجديد
- تبويب "كشف الحساب" يعرض القيود الفعلية بدلاً من قائمة فارغة
- إصلاح `amountEgp` يُرسل كـ number بدلاً من string في موضعين
- إصلاح خطأ validation: `partyTypeSnapshot` أصبح اختيارياً في الـ schema
- إصلاح `CreatePartyReturnDialog`: كانت تُرسل `invoiceId` بدلاً من `sourceInvoiceId`

**الملفات المُعدَّلة**:
- `server/routes.ts` (+13 سطر)
- `server/storage.ts` (+58 سطر)
- `shared/schema.ts` (تعديل)
- `client/src/hooks/use-local-trade.ts` (+16 سطر)
- `client/src/pages/local-trade/parties.tsx` (تعديل)
- `client/src/pages/local-trade/party-profile.tsx` (تعديل)
- `replit.md` (تحديث)

---

### 6. حالة السداد عند إنشاء الفاتورة + ربط المدفوعات (12:43 م)
**الهدف**: السماح بتسجيل دفعة مبدئية أثناء إنشاء الفاتورة  
**ما تم**:

**الفرونت (`create-invoice.tsx`)**:
- إضافة قسم "حالة السداد" في نهاية نموذج الفاتورة
- 3 خيارات: غير مدفوع / مدفوع جزئياً / مدفوع بالكامل
- عند اختيار جزئي أو كامل: يظهر حقل المبلغ وطريقة الدفع
- يُنشأ سجل دفع تلقائياً مرتبط بالفاتورة عند الحفظ

**الباك (`server/routes.ts`)**:
- مسار إنشاء الفاتورة يقبل كائن `initialPayment` اختياري
- يُنفَّذ atomically: إنشاء الفاتورة + تسجيل الدفع في نفس العملية

**إصلاح طريقة الدفع (`party-profile.tsx`)**:
- قيم طرق الدفع أصبحت عربية: نقدي / فودافون كاش / إنستاباي / تحويل بنكي / شيك / أخرى
- إصلاح `entryType: 'credit'` → `'payment'` في سجل الـ ledger

**الملفات المُعدَّلة**:
- `client/src/pages/local-trade/create-invoice.tsx` (+120 سطر)
- `server/routes.ts` (+21 سطر)
- `server/storage.ts` (تعديل)

---

### 7. التسوية التلقائية بـ FIFO + حل حالات الهامش (12:58 م)
**الهدف**: ربط المدفوعات بالفواتير تلقائياً بطريقة الأقدم أولاً  
**ما تم**:

**قاعدة البيانات (`shared/schema.ts`)**:
- جدول جديد `local_invoice_allocations`:
  - `invoiceId` → الفاتورة
  - `amountEgp` → المبلغ المخصص
  - `sourceType`: `'payment'` أو `'return_deduction'`
  - `paymentId` (nullable) أو `returnCaseId` (nullable)

**الباك (`server/storage.ts`)**:
- `createLocalPayment`: بعد تسجيل الدفع، يوزّع المبلغ تلقائياً على أقدم الفواتير غير المسدّدة (FIFO)
- `getAllLocalInvoicesWithPayments`: يقرأ `paidAmount` من `local_invoice_allocations` + الروابط المباشرة
- `resolveReturnCase` بـ `deduct_value` أو `accepted_return`: يُنشئ allocation يُقلل رصيد الفاتورة المصدر
- `getLocalPayments`: يُعيد المدفوعات مع مصفوفة `allocations[]` تُبيّن الفواتير المُسوَّاة

**الفرونت (`party-profile.tsx`)**:
- `PaymentsTab`: كل دفعة تعرض الفواتير المُسوَّاة (badges بالمبالغ)
- `ReturnsTab`: زر "تسوية" لكل حالة هامش `under_inspection`
- مكوّن `ResolveReturnCaseDialog`: نافذة تسوية الهامش بـ 5 خيارات:
  1. **خصم من الفاتورة** → يُقلل رصيد الفاتورة مباشرة
  2. **مرتجع مقبول** → يُسجّل استرداد ويُقلل الرصيد
  3. **استبدال** → إعادة إرسال البضاعة
  4. **تالف** → شطب خسارة
  5. **مرفوض** → رفض الادعاء

**hooks**:
- `useResolveReturnCase`: يُعيد تحديث 4 caches بعد التسوية (الهوامش + الفواتير + المدفوعات + الأطراف)

**الملفات المُعدَّلة**:
- `shared/schema.ts` (+29 سطر)
- `server/storage.ts` (+138 سطر)
- `client/src/pages/local-trade/party-profile.tsx` (+290 سطر)
- `client/src/hooks/use-local-trade.ts` (+3 سطر)
- `replit.md` (تحديث)

---

### 8. تحسين تبويب الفواتير + فلترة (1:09 م)
**الهدف**: واجهة أوضح وأكثر وظيفية لتبويب الفواتير في البروفايل  
**ما تم**:

**`InvoicesTab` في (`party-profile.tsx`)**:
- فلترة بالحالة: الكل / مسودة / معلقة / مستلمة جزئياً / مستلمة / ملغاة
- أيقونات توضيحية لكل نوع فاتورة (شراء/بيع/بيع بدون مخزون)
- أزرار إجراءات لكل فاتورة: عرض / استلام (إن كانت في انتظار استلام)
- عرض: الإجمالي، المدفوع، المتبقي، حالة السداد في كل صف
- تكامل مع `ViewInvoiceDialog` و`ReceiveInvoiceDialogLocal`

**الباك (`storage.ts`)**:
- تعديل فلتر FIFO ليتجاهل الفواتير المسدّدة بالكامل عند توزيع الدفعات الجديدة

**الملفات المُعدَّلة**:
- `client/src/pages/local-trade/party-profile.tsx` (+366 سطر)
- `server/storage.ts` (تعديل)

---

### 9. تحديث حساب المدفوع والمتبقي (1:16 م)
**الهدف**: إصلاح حساب paidAmount ليشمل المدفوعات المباشرة والـ allocations  
**ما تم**:

**الباك (`storage.ts`)**:
- `getAllLocalInvoicesWithPayments`: الاستعلام الآن يجمع:
  1. المدفوعات المرتبطة مباشرة (`localPayments.invoiceId = invoice.id`)
  2. الـ allocations من جدول `local_invoice_allocations`
- تجنب الازدواجية عند حساب الإجمالي
- حساب `remainingAmount` و`paymentStatus` بدقة

**الفرونت (`party-profile.tsx`)**:
- `PaymentDialog`: يعرض **كل** الفواتير (مسودة + معلقة + مستلمة) بدلاً من المعلقة فقط، للسماح بالسداد المسبق

**الملفات المُعدَّلة**:
- `server/storage.ts` (+92 سطر)
- `client/src/pages/local-trade/party-profile.tsx` (+12 سطر)

---

### 10. طباعة إيصال الدفع (POS) (1:32 م)
**الهدف**: إضافة إيصال طباعة لكل دفعة بتنسيق POS  
**ما تم**:

**مكوّن `PaymentReceiptDialog` الجديد**:
- نافذة عرض إيصال الدفعة بتفاصيل كاملة:
  - اسم الطرف، نوعه، تاريخ الدفعة
  - المبلغ المدفوع، طريقة الدفع، رقم المرجع
  - جدول الفواتير المُسوَّاة (من FIFO allocations)
  - ملخص الحساب: الإجمالي المستحق، المدفوع، الرصيد المتبقي
- زر طباعة: يفتح نافذة طباعة منفصلة بعرض 80mm (مناسب لطابعات POS/كاشير)
- CSS مخصص للطباعة: RTL، خط صغير، لا ألوان زائدة

**تكامل في `PaymentsTab`**:
- أيقونة طباعة 🖨️ في كل صف من صفوف المدفوعات
- عند الضغط: يفتح `PaymentReceiptDialog` لتلك الدفعة

**الملفات المُعدَّلة**:
- `client/src/pages/local-trade/party-profile.tsx` (+307 سطر)

---

### 11. إصلاح خطأ FK عند استلام الفاتورة (1:51 م)
**الهدف**: إصلاح خطأ قاعدة بيانات كان يمنع استلام الفواتير نهائياً  
**المشكلة**:
- `receiveInvoice` في الباك كان يحاول إنشاء سجل في `inventory_movements`
- جدول `inventory_movements` يرتبط بجدول `products` الخاص بوحدة الشحنات
- التجارة المحلية تستخدم `product_types` مختلفة → FK constraint violation

**الإصلاح**:
- حذف كامل كود `createInventoryMovement` من `receiveInvoice`
- التجارة المحلية تتبع الكميات عبر `receivedPieces` على بنود الفاتورة مباشرة
- لا حاجة لجدول inventory_movements في هذه الوحدة

**الملفات المُعدَّلة**:
- `server/storage.ts` (−11 سطر)

---

### 12. تجديد صفحة قائمة الأطراف (1:57 م)
**الهدف**: تصميم أحدث وتجربة مستخدم أفضل  
**ما تم**:

**تصميم جديد كلياً (`parties.tsx`)**:
- من: جدول تقليدي بـ 10 أعمدة مزدحم
- إلى: صفوف بطاقية واسعة مريحة للعين

**المميزات الجديدة**:
- **أفاتار** يعرض الحرف الأول من الاسم (ملوّن حسب النوع)
- **الضغط على أي جزء من الصف** ينقل لبروفايل الطرف مباشرة
- **سطر البيانات**: الاسم + اسم المحل + الهاتف + العنوان في سطر واحد منظّم
- **الرصيد**: بارز وملوّن (أحمر = عليه / أخضر = له)
- **زر التعديل**: يظهر فقط عند مرور الماوس (hover) ولا يُعيق الضغط للانتقال
- **شريط نوع الطرف**: تاجر / عميل / مزدوج بألوان مختلفة

**الملفات المُعدَّلة**:
- `client/src/pages/local-trade/parties.tsx` (−246 +173 = إعادة كتابة جوهرية)

---

### 13. إصلاح عرض النواقص في الفاتورة المستلمة (2:02 م)
**الهدف**: إظهار النواقص وربطها بتبويب الهوامش  
**المشاكل التي تم إصلاحها**:

**أ. النواقص لا تظهر في تبويب الهوامش بعد الاستلام**
- السبب: `useReceiveInvoice` كان يُحدّث cache الفواتير فقط
- الإصلاح: يُحدّث الآن 3 caches: الفواتير + **الهوامش** + الأطراف

**ب. عرض الفاتورة المستلمة لا يُظهر الكمية الفعلية**
- السبب: `ViewInvoiceDialog` لا يحتوي على أعمدة المستلم/النواقص
- الإصلاح: عند الفاتورة المستلمة (`status = received`) تظهر:
  - عمود "المطلوب" (الكمية الأصلية)
  - عمود "المستلم" (باللون الأخضر)
  - عمود "النواقص" (badge أحمر / تمام ✓)
  - تنبيه أسفل الجدول: "يوجد نواقص — راجع تبويب الهوامش"

**ج. نوع الطرف مُثبّت بـ 'merchant' في حالات الهامش التلقائية**
- السبب: `receiveInvoice` في الباك يُثبّت `partyTypeSnapshot: 'merchant'`
- الإصلاح: يجلب نوع الطرف الفعلي من قاعدة البيانات

**د. مبلغ الهامش مُخفي قبل التسوية**
- السبب: `ReturnsTab` يُظهر `amountEgp` فقط إذا كانت الحالة `resolved`
- الإصلاح: المبلغ يظهر دائماً (أصفر = تحت الفحص، أخضر = تم الحل)

**الملفات المُعدَّلة**:
- `client/src/hooks/use-local-trade.ts` (+2 سطر)
- `client/src/pages/local-trade/party-profile.tsx` (+54 سطر)
- `server/storage.ts` (+6 سطر)

---

## ملخص الملفات المُعدَّلة اليوم

| الملف | التعديلات |
|---|---|
| `client/src/pages/local-trade/party-profile.tsx` | +1,160 سطر — الملف الأكثر تعديلاً |
| `client/src/pages/local-trade/create-invoice.tsx` | +182 سطر |
| `client/src/pages/local-trade/parties.tsx` | إعادة كتابة جوهرية |
| `server/storage.ts` | +243 سطر |
| `server/routes.ts` | +34 سطر |
| `shared/schema.ts` | +33 سطر (جدول جديد) |
| `client/src/hooks/use-local-trade.ts` | +21 سطر |
| `replit.md` | +29 سطر (توثيق) |

---

## الميزات المُضافة اليوم (ملخص)

| # | الميزة | الحالة |
|---|---|---|
| 1 | توثيق PRD كامل (TXT + DOCX) | ✅ مكتمل |
| 2 | نوع فاتورة "بيع بدون مخزون" | ✅ مكتمل |
| 3 | هامش يدوي من بروفايل الطرف | ✅ مكتمل |
| 4 | إصلاح أرصدة الأطراف من الـ Ledger | ✅ مكتمل |
| 5 | كشف حساب فعلي بالقيود | ✅ مكتمل |
| 6 | حالة السداد عند إنشاء الفاتورة | ✅ مكتمل |
| 7 | التسوية التلقائية FIFO للمدفوعات | ✅ مكتمل |
| 8 | جدول `local_invoice_allocations` | ✅ مكتمل |
| 9 | تسوية حالات الهامش (5 خيارات) | ✅ مكتمل |
| 10 | إيصال طباعة POS لكل دفعة | ✅ مكتمل |
| 11 | إصلاح خطأ FK عند الاستلام | ✅ مكتمل |
| 12 | إعادة تصميم صفحة قائمة الأطراف | ✅ مكتمل |
| 13 | عرض النواقص في الفاتورة المستلمة | ✅ مكتمل |
| 14 | ربط النواقص بتبويب الهوامش تلقائياً | ✅ مكتمل |
| 15 | إصلاح partyTypeSnapshot في الهوامش التلقائية | ✅ مكتمل |

---

## الـ Bugs المُصلَحة اليوم

| # | المشكلة | السبب الجذري | الإصلاح |
|---|---|---|---|
| 1 | رصيد الطرف = 0 دائماً | قراءة من حقل غير موجود | قراءة من ledger مباشرة |
| 2 | كشف الحساب فارغ | لا يوجد API endpoint | إضافة `/parties/:id/ledger` |
| 3 | FK error عند استلام الفاتورة | استدعاء `inventory_movements` من وحدة الشحنات | حذف الاستدعاء نهائياً |
| 4 | `partyTypeSnapshot` = 'merchant' دائماً | hardcoded في `receiveInvoice` | جلب النوع من DB |
| 5 | النواقص لا تظهر في الهوامش | `useReceiveInvoice` لا يُحدّث cache الهوامش | إضافة invalidation للهوامش |
| 6 | مبلغ الهامش مخفي | عرض شرطي خاطئ | عرض للجميع مع ألوان مختلفة |
| 7 | طريقة دفع إنجليزية في قاعدة البيانات | قيم hardcoded بالإنجليزية | تحويل لقيم عربية |
| 8 | `amountEgp` يُرسل كـ string | نوع بيانات خاطئ | إرسال كـ number |

---

*تقرير مُنشأ تلقائياً — Tracker System — 2026-05-02*
